# Chat-system dependencies (Python 3.10+).
sentence-transformers>=2.7   # PubMedBERT-family embeddings (pulls torch, transformers)
sqlite-vec>=0.1.3            # vector search inside SQLite
numpy
pytest>=8.0

anthropic>=0.69             # Claude Opus 4.8 generation + web_search (Phase 3)
python-dotenv>=1.0          # load ANTHROPIC_API_KEY from .env (server-side only)
fastapi>=0.115              # /chat SSE backend (Phase 4)
uvicorn>=0.29               # ASGI server for the backend
httpx>=0.27                 # FastAPI TestClient (tests)
